name: Docker Buildup

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - name: Build the Docker image
      run: docker build --file Dockerfile --tag gliese129/bililive-recorder:latest .
